# Root-level Environment Variables for the Monorepo
# This file serves as an example of the environment variables needed at the root level
# Copy this file to .env, .env.development, .env.staging, or .env.production as needed

# Common variables
NODE_ENV=development  # Can be development, staging, or production

# Supabase variables - used by the monorepo
# These are currently used by the monorepo setup scripts
EXPO_PUBLIC_SUPABASE_URL=http://localhost:54321  # For local: http://localhost:54321, For production: https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key       # Your Supabase anon/public key (safe to include in client code)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key   # Your Supabase service role key (DO NOT expose in client code)

# For deployment 
SUPABASE_PROJECT_REF=your_project_ref  # The reference ID of your Supabase project for CLI commands